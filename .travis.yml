language: python
python:
  - "3.5" 
# command to install dependencies
# install: "pip install -r requirements.txt"
# command to run tests
script:
  - py.test teste.py

after script:
notifications:
  recipients:
    - user.teste.atividade@gmail.com

